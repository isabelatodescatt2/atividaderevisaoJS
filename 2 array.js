const animais = ["gato", "cachorro", "coelho", "macaco", "coala"];
// Percorrendo um array utilizando loop for
for (let i = 0; i < animais.length; i++) {
  console.log(`Índice ${i} = ${animais[i]}`);
}
